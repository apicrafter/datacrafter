---
title: "clean"
description: "Remove temporary pipeline files"
---

# clean

```bash
datacrafter clean [--path PATH] [--storage] [--verbose]
```

Removes latest temporary files. `--storage` also cleans the `storage/`
directory (for example APIBackuper working data).
