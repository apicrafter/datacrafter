---
title: "metrics"
description: "Record counts and field histograms from JSONL output"
---

# metrics

```bash
datacrafter metrics [--path PATH]
```

Same file discovery as [`schema`](/commands/schema): JSONL under `output/` or
`current/`. Prints record counts and per-field histograms as YAML.
