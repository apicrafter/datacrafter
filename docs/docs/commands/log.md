---
title: "log"
description: "Show the latest pipeline log"
---

# log

```bash
datacrafter log [--path PATH] [--lines N] [--verbose]
```

Prints the tail of `datacrafter.log` (`-n` / `--lines` defaults to 50).
